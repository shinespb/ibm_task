define service {
        hostgroup_name                  http-servers
        service_description             PATTERNS
        check_command                   check_patterns_nrpe!check_patterns!{{ patterns_path }}!{{patterns_warning}}!{{patterns_critical}}
        use                             generic-service
        notification_interval           0 ; set > 0 if you want to be renotified
}